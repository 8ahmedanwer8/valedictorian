import React from "react";
import { VStack, Text, Box } from "@chakra-ui/react";
const RatingsPanel = () => {
  return <Box alignItems="flex-start" width="full" height="fit-content"></Box>;
};

export default RatingsPanel;
